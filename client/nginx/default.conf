server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location ~ ^.+\..+$ {
      try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}