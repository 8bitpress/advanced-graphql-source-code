import { Button } from "grommet";
import React from "react";

const LoadMoreButton = props => (
  <Button label="Load More" margin={{ top: "medium" }} primary {...props} />
);

export default LoadMoreButton;
